package demo02;

public class VersionDemo {
    public VersionDemo(int version1, int version2) {
        System.out.println((version1 > version2) ? "Latest: " + version1 : "Latest: " + version2);
    }
}
